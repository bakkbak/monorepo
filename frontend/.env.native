# Loaded only by native builds via `--mode native` (npm run build:native).
# The WebView origin is capacitor://localhost, so API calls must target the
# deployed backend by absolute URL.
VITE_API_BASE=https://teevo.in/api
