node_modules/
dist/
.DS_Store
__pycache__/
*.pyc
.env
.env.*
# Native build config holds a public API URL, not secrets — keep it tracked so
# `npm run build:native` works for anyone who clones the repo.
!frontend/.env.native
venv/
.venv/
*.db
*.db-shm
*.db-wal
.vercel/

# Android Studio IDE files
frontend/android/.idea/
